<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mesaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".moreInfo"></activity>
        <activity android:name=".statPage" />
        <activity android:name=".plotPage" />
        <activity android:name=".dataPage" />
        <activity android:name=".updateHeight" />
        <activity android:name=".updatePassword" />
        <activity android:name=".settingsPage" />
        <activity android:name=".sleepPage" />
        <activity android:name=".secondaryAct" />
        <activity android:name=".signUpPage" />
        <activity android:name=".caloriesPage" />
        <activity android:name=".weightPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>